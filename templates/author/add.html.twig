{% extends 'base.html.twig' %}

{% block title %}Add Author{% endblock %}

{% block body %}
    <h1>Add Author</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">Save</button>
    {{ form_end(form) }}
{% endblock %}
